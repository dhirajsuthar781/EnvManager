( SHARE FLOW )

OWNER - CREATE PROJECT 
        DELETE PROJECT
        UPDATE PROJECT
        VIEW PROJECT DETAILS
        SHARE PROJECT
        CREATE ENV 
        UPDATE ENV
        REMOVE ENV

SHARED USER - VIEW PROJECT ( CAN'T DELETE/UPDATE/SHARE)
              - CREATE ENV ( EDITOR ROLE ONLY )
              - UPDATE ENV ( EDITOR ROLE ONLY )
              - REMOVE ENV ( EDITOR ROLE ONLY )
              - VIEW ENV DETAILS ( BY DEFAULT )


